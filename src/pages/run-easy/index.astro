---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<BaseLayout
  title="Run Easy Z2 — Heart Rate Zone Training for iOS & Apple Watch"
  description="Heart rate zone training built around the Maffetone method. Real-time coaching on your wrist, training analytics on your phone."
  ogImage="/img/run-easy/icon.png"
  ogUrl="https://idlefusion.com/run-easy"
>
  <Header />

  <main>
    <!-- Hero -->
    <section class="section-padding">
      <div class="container-custom">
        <div class="hero-layout">
          <div class="hero-text">
            <h1 class="hero-title">
              Run easy,<br/>train <em>smarter.</em>
            </h1>
            <p class="hero-subtitle">
              Heart rate zone training built around the Maffetone method. Real-time coaching on your wrist, training analytics on your phone.
            </p>
            <div class="hero-actions">
              <a href="#" class="app-store-badge">
                <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
              </a>
            </div>
          </div>
          <div class="hero-icon">
            <img src="/img/run-easy/icon.png" alt="Run Easy Z2 app icon" class="icon-img" width="240" height="240" />
          </div>
        </div>
      </div>
    </section>

    <div class="section-divider"></div>

    <!-- Features -->
    <section class="section-padding">
      <div class="container-custom">
        <div class="features-header">
          <span class="section-number">01 — FEATURES</span>
          <h2 class="features-title">Stay in the zone.</h2>
        </div>

        <div class="features-grid">
          <div class="feature">
            <h3 class="feature-heading">Real-Time Zone Coaching</h3>
            <p>Your Apple Watch shows your current HR zone live &mdash; with haptic and audio cues when you drift out of your target.</p>
          </div>

          <div class="feature">
            <h3 class="feature-heading">MAF 180 Built In</h3>
            <p>Enter your age, get your zones. The Maffetone method removes the guesswork from aerobic base building.</p>
          </div>

          <div class="feature">
            <h3 class="feature-heading">Training Analytics</h3>
            <p>Cardiac drift, HR recovery, zone distribution, and weekly trends &mdash; see your aerobic fitness improve over time.</p>
          </div>

          <div class="feature">
            <h3 class="feature-heading">iPhone + Apple Watch</h3>
            <p>Start runs from your wrist. Review history, stats, and settings on your phone. Everything syncs automatically.</p>
          </div>

          <div class="feature">
            <h3 class="feature-heading">Live Activities</h3>
            <p>Track your zone from the lock screen and Dynamic Island without raising your wrist or opening the app.</p>
          </div>

          <div class="feature">
            <h3 class="feature-heading">Completely Private</h3>
            <p>No accounts, no tracking, no analytics. Your data stays on your device and syncs only to your own iCloud.</p>
          </div>
        </div>
      </div>
    </section>

    <div class="section-divider"></div>

    <!-- Screenshots -->
    <section class="section-padding">
      <div class="container-custom">
        <div class="screenshots-header">
          <span class="section-number">02 — SCREENSHOTS</span>
          <h2 class="screenshots-title">See it in action.</h2>
        </div>

        <div class="screenshots-row">
          <div class="screenshot-item screenshot-phone">
            <img src="/img/run-easy/iphone-history.png" alt="Run history on iPhone" loading="lazy" />
            <span class="screenshot-caption">Run History</span>
          </div>
          <div class="screenshot-item screenshot-phone">
            <img src="/img/run-easy/iphone-detail.png" alt="Run detail on iPhone" loading="lazy" />
            <span class="screenshot-caption">Run Detail</span>
          </div>
          <div class="screenshot-item screenshot-phone">
            <img src="/img/run-easy/iphone-settings.png" alt="Settings on iPhone" loading="lazy" />
            <span class="screenshot-caption">Settings</span>
          </div>
          <div class="screenshot-item screenshot-watch">
            <img src="/img/run-easy/watch-home.png" alt="Watch home screen" loading="lazy" />
            <span class="screenshot-caption">Watch Home</span>
          </div>
          <div class="screenshot-item screenshot-watch">
            <img src="/img/run-easy/watch-workout.png" alt="Active workout on Apple Watch" loading="lazy" />
            <span class="screenshot-caption">Active Workout</span>
          </div>
        </div>
      </div>
    </section>

    <div class="section-divider"></div>

    <!-- CTA -->
    <section class="section-padding">
      <div class="container-custom">
        <div class="cta-section">
          <h2 class="cta-title">Start running easier.</h2>
          <p class="cta-text">Free on the App Store. No account required.</p>
          <div class="cta-badge">
            <a href="#" class="app-store-badge">
              <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
            </a>
          </div>
          <div class="cta-links">
            <a href="/run-easy/support" class="link-accent">Support</a>
            <span class="cta-dot">&middot;</span>
            <a href="/run-easy/privacy" class="link-accent">Privacy Policy</a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  /* Hero */
  .hero-layout {
    display: flex;
    flex-direction: column-reverse;
    align-items: center;
    gap: 3rem;
    text-align: center;
  }

  @media (min-width: 768px) {
    .hero-layout {
      flex-direction: row;
      text-align: left;
      gap: 4rem;
    }
  }

  .hero-text {
    flex: 1;
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    color: var(--text-primary);
    margin-bottom: 1.5rem;
  }

  .hero-title em {
    font-style: italic;
  }

  .hero-subtitle {
    font-size: 1.125rem;
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 480px;
    margin-bottom: 2rem;
  }

  @media (max-width: 767px) {
    .hero-subtitle {
      margin-left: auto;
      margin-right: auto;
    }
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  @media (min-width: 768px) {
    .hero-actions {
      justify-content: flex-start;
    }
  }

  .app-store-badge img {
    height: 54px;
    transition: opacity 0.2s;
  }

  .app-store-badge:hover img {
    opacity: 0.8;
  }

  .hero-icon {
    flex-shrink: 0;
  }

  .icon-img {
    width: 180px;
    height: 180px;
    border-radius: 40px;
    box-shadow: var(--shadow-lg);
  }

  @media (min-width: 768px) {
    .icon-img {
      width: 240px;
      height: 240px;
      border-radius: 54px;
    }
  }

  /* Features */
  .features-header {
    margin-bottom: 3rem;
  }

  .features-title {
    font-size: clamp(1.75rem, 4vw, 3rem);
    color: var(--text-primary);
    margin-top: 1rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 640px) {
    .features-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (min-width: 1024px) {
    .features-grid {
      grid-template-columns: 1fr 1fr 1fr;
    }
  }

  .feature {
    padding: 2rem;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: 3px;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .feature:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
    border-color: var(--accent);
  }

  .feature-heading {
    font-family: 'Satoshi', sans-serif;
    font-size: 1.125rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
    letter-spacing: 0;
    line-height: 1.4;
  }

  .feature p {
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* Screenshots */
  .screenshots-header {
    margin-bottom: 3rem;
  }

  .screenshots-title {
    font-size: clamp(1.75rem, 4vw, 3rem);
    color: var(--text-primary);
    margin-top: 1rem;
  }

  .screenshots-row {
    display: flex;
    gap: 1.5rem;
    overflow-x: auto;
    padding-bottom: 1rem;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
  }

  .screenshots-row::-webkit-scrollbar {
    height: 6px;
  }

  .screenshots-row::-webkit-scrollbar-track {
    background: var(--bg-secondary);
    border-radius: 3px;
  }

  .screenshots-row::-webkit-scrollbar-thumb {
    background: var(--border-color);
    border-radius: 3px;
  }

  .screenshot-item {
    flex-shrink: 0;
    scroll-snap-align: start;
    text-align: center;
  }

  .screenshot-phone {
    width: 220px;
  }

  .screenshot-watch {
    width: 160px;
  }

  .screenshot-item img {
    width: 100%;
    height: auto;
    border-radius: 12px;
    box-shadow: var(--shadow-md);
    border: 1px solid var(--border-color);
  }

  .screenshot-caption {
    display: block;
    margin-top: 0.75rem;
    font-size: 0.8125rem;
    color: var(--text-muted);
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 0.02em;
  }

  /* CTA */
  .cta-section {
    text-align: center;
    max-width: 480px;
    margin: 0 auto;
  }

  .cta-badge {
    display: flex;
    justify-content: center;
  }

  .cta-title {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--text-primary);
    margin-bottom: 1rem;
  }

  .cta-text {
    font-size: 1.125rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .cta-links {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    margin-top: 2rem;
    font-size: 0.875rem;
  }

  .cta-dot {
    color: var(--text-muted);
  }
</style>
