---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
import ExploreApps from '../../components/ExploreApps.astro';
---

<BaseLayout
  title="Contrast — Sauna + Cold Plunge Timer for iOS"
  description="A simple, purpose-built timer for contrast therapy. HealthKit integration, smart protocols, Apple Watch support. No accounts, no subscriptions."
  ogImage="/img/contrast/icon.png"
  ogUrl="https://idlefusion.com/contrast"
>

  <!-- App Nav — sits on dark hero -->
  <nav class="ct-nav">
    <div class="ct-nav-inner">
      <a href="/contrast" class="ct-logo">Contrast</a>
      <div class="ct-nav-actions">
        <ThemeToggle />
        <a href="https://apps.apple.com/app/id6759070330" class="ct-nav-cta" target="_blank" rel="noopener noreferrer">
          <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="40" />
        </a>
      </div>
    </div>
  </nav>

  <main>
    <!-- Hero — Dark, bold, dramatic -->
    <section class="ct-hero">
      <div class="ct-hero-glow ct-hero-glow--warm"></div>
      <div class="ct-hero-glow ct-hero-glow--cool"></div>
      <div class="ct-hero-inner">
        <div class="ct-hero-content">
          <div class="ct-hero-text">
            <div class="ct-hero-badge">SAUNA + COLD PLUNGE</div>
            <h1 class="ct-hero-title">
              Contrast therapy,<br/><em>timed.</em>
            </h1>
            <p class="ct-hero-subtitle">
              A purpose-built timer for sauna and cold plunge sessions. HealthKit sync, smart protocols, Apple Watch support.
            </p>
            <div class="ct-hero-actions">
              <a href="https://apps.apple.com/app/id6759070330" class="ct-store-badge" target="_blank" rel="noopener noreferrer">
                <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
              </a>
              <span class="ct-hero-note">Free &middot; No account required</span>
            </div>
          </div>
          <div class="ct-hero-icon-wrap">
            <img src="/img/contrast/icon.png" alt="Contrast app icon" class="ct-hero-icon" width="240" height="240" />
          </div>
        </div>
      </div>
    </section>

    <!-- Screenshots Gallery -->
    <section class="ct-screenshots">
      <div class="ct-screenshots-inner">
        <div class="ct-screenshots-scroll">
          <img src="/img/contrast/screenshots/01_home.png" alt="Contrast home screen" class="ct-screenshot" />
          <img src="/img/contrast/screenshots/02_active_sauna.png" alt="Active sauna timer" class="ct-screenshot" />
          <img src="/img/contrast/screenshots/03_active_cold.png" alt="Active cold plunge timer" class="ct-screenshot" />
          <img src="/img/contrast/screenshots/04_multi_phase.png" alt="Multi-phase protocol" class="ct-screenshot" />
          <img src="/img/contrast/screenshots/05_session_complete.png" alt="Session complete" class="ct-screenshot" />
          <img src="/img/contrast/screenshots/06_personal_records.png" alt="Personal records" class="ct-screenshot" />
          <img src="/img/contrast/screenshots/07_history.png" alt="Session history" class="ct-screenshot" />
        </div>
      </div>
    </section>

    <!-- Features — Dark bg, two-column with large cards -->
    <section class="ct-features-section">
      <div class="ct-container-wide">
        <div class="ct-features-header">
          <span class="ct-label-dark">FEATURES</span>
          <h2 class="ct-features-title">Built for the session.</h2>
        </div>

        <div class="ct-features">
          <div class="ct-feature">
            <div class="ct-feature-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
            </div>
            <h3>Quick Start</h3>
            <p>Two taps to start timing. Sauna or cold plunge — pick one and go. Designed for wet hands and foggy glasses.</p>
          </div>

          <div class="ct-feature">
            <div class="ct-feature-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12h4l3-9 6 18 3-9h4"/></svg>
            </div>
            <h3>Smart Protocols</h3>
            <p>Multi-phase session plans that guide you through sauna, cold plunge, and rest intervals with automatic transitions.</p>
          </div>

          <div class="ct-feature">
            <div class="ct-feature-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg>
            </div>
            <h3>HealthKit Sync</h3>
            <p>Sessions save to Apple Health as workouts automatically. Your contrast therapy shows up alongside everything else you track.</p>
          </div>

          <div class="ct-feature">
            <div class="ct-feature-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M18 20V10"/><path d="M12 20V4"/><path d="M6 20v-6"/></svg>
            </div>
            <h3>Session History</h3>
            <p>Every session is saved. Track personal records, total exposure time, and build the consistency that drives results.</p>
          </div>

          <div class="ct-feature">
            <div class="ct-feature-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="2"/><path d="M12 18h.01"/></svg>
            </div>
            <h3>Apple Watch</h3>
            <p>Start, pause, and advance phases from your wrist. Haptic alerts tell you when it's time to switch — no phone needed.</p>
          </div>

          <div class="ct-feature">
            <div class="ct-feature-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
            </div>
            <h3>Completely Offline</h3>
            <p>No servers, no cloud sync, no network requests. Your data stays on your device. Period.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Privacy — Full-width dark banner -->
    <section class="ct-privacy-banner">
      <div class="ct-container">
        <div class="ct-privacy-inner">
          <span class="ct-label-dark">PRIVACY</span>
          <h2 class="ct-privacy-title">Zero data collection.</h2>
          <p class="ct-privacy-text">
            No analytics. No ads. No tracking pixels. No telemetry.<br/>
            Contrast doesn't even make network requests. Your session data lives on your device and nowhere else.
          </p>
          <a href="/contrast/privacy" class="ct-privacy-link">Read the full privacy policy &rarr;</a>
        </div>
      </div>
    </section>

    <!-- CTA — Dark section -->
    <section class="ct-cta">
      <div class="ct-container">
        <div class="ct-cta-inner">
          <h2 class="ct-cta-title">Start your session.</h2>
          <p class="ct-cta-text">Free on the App Store. No account required.</p>
          <a href="https://apps.apple.com/app/id6759070330" class="ct-store-badge" target="_blank" rel="noopener noreferrer">
            <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
          </a>
        </div>
      </div>
    </section>
    <ExploreApps current="contrast" />
  </main>

  <!-- App Footer -->
  <footer class="ct-footer">
    <div class="ct-footer-inner">
      <div class="ct-footer-links">
        <a href="/contrast/support">Support</a>
        <span class="ct-footer-dot">&middot;</span>
        <a href="/contrast/privacy">Privacy Policy</a>
      </div>
      <div class="ct-footer-brand">
        An <a href="/">Idle Fusion</a> app
      </div>
    </div>
  </footer>

</BaseLayout>

<style>
  /* ═══════════════════════════════════════════════════════
     CONTRAST — Teal / Cyan, Dark Immersive Theme
     Bold, elemental, fire & ice
     ═══════════════════════════════════════════════════════ */

  :global(:root) {
    --ct-accent: #06b6d4;
    --ct-accent-deep: #0891b2;
    --ct-accent-bg: #ecfeff;
    --ct-warm: #f97316;
    --ct-bg: #f8fafc;
    --ct-surface: #ffffff;
    --ct-border: rgba(148, 163, 184, 0.15);
    --ct-text: #0f172a;
    --ct-text-muted: #64748b;
    --ct-feature-icon-bg: rgba(6, 182, 212, 0.08);
    --ct-feature-icon-border: rgba(6, 182, 212, 0.12);
  }

  :global(.dark) {
    --ct-accent: #22d3ee;
    --ct-accent-deep: #06b6d4;
    --ct-accent-bg: #083344;
    --ct-bg: #0a0f1a;
    --ct-surface: #111827;
    --ct-border: rgba(148, 163, 184, 0.08);
    --ct-text: #f1f5f9;
    --ct-text-muted: #94a3b8;
    --ct-feature-icon-bg: rgba(6, 182, 212, 0.1);
    --ct-feature-icon-border: rgba(6, 182, 212, 0.15);
  }

  /* ─── Nav ─── */
  .ct-nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: var(--ct-bg);
    border-bottom: 1px solid var(--ct-border);
    backdrop-filter: blur(12px);
  }

  .ct-nav-inner {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 3.5rem;
  }

  @media (min-width: 640px) {
    .ct-nav-inner { padding: 0 2rem; }
  }

  @media (min-width: 1024px) {
    .ct-nav-inner { padding: 0 3rem; }
  }

  .ct-logo {
    font-family: 'Satoshi', sans-serif;
    font-size: 0.9375rem;
    font-weight: 700;
    color: var(--ct-accent-deep);
    text-decoration: none;
    letter-spacing: -0.01em;
  }

  .ct-nav-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .ct-nav-cta img {
    height: 32px;
    transition: opacity 0.2s;
  }

  .ct-nav-cta:hover img {
    opacity: 0.7;
  }

  /* ─── Hero — Dark, dramatic, dual-glow (stays dark in both modes) ─── */
  .ct-hero {
    background: #0a0f1a;
    padding: 4rem 0 5rem;
    position: relative;
    overflow: hidden;
  }

  .ct-hero-glow {
    position: absolute;
    width: 500px;
    height: 500px;
    border-radius: 50%;
    pointer-events: none;
    filter: blur(120px);
    opacity: 0.12;
  }

  .ct-hero-glow--warm {
    top: -30%;
    left: -10%;
    background: var(--ct-warm);
  }

  .ct-hero-glow--cool {
    bottom: -40%;
    right: -10%;
    background: var(--ct-accent);
  }

  .ct-hero-inner {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    position: relative;
  }

  @media (min-width: 640px) {
    .ct-hero-inner { padding: 0 2rem; }
  }

  @media (min-width: 1024px) {
    .ct-hero-inner { padding: 0 3rem; }
  }

  .ct-hero-content {
    display: flex;
    flex-direction: column-reverse;
    align-items: center;
    gap: 3rem;
    text-align: center;
  }

  @media (min-width: 768px) {
    .ct-hero-content {
      flex-direction: row;
      text-align: left;
      gap: 4rem;
    }
  }

  .ct-hero-text { flex: 1; }

  .ct-hero-badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6875rem;
    letter-spacing: 0.12em;
    color: var(--ct-accent);
    margin-bottom: 1.5rem;
  }

  .ct-hero-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    color: #f1f5f9;
    margin-bottom: 1.5rem;
    line-height: 1.05;
  }

  .ct-hero-title em {
    font-style: italic;
    color: var(--ct-accent);
  }

  .ct-hero-subtitle {
    font-size: 1.125rem;
    color: #94a3b8;
    line-height: 1.7;
    max-width: 480px;
    margin-bottom: 2rem;
  }

  @media (max-width: 767px) {
    .ct-hero-subtitle { margin-left: auto; margin-right: auto; }
  }

  .ct-hero-actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }

  @media (min-width: 768px) {
    .ct-hero-actions { align-items: flex-start; }
  }

  .ct-store-badge {
    display: inline-block;
  }

  .ct-store-badge img {
    height: 54px;
    transition: opacity 0.2s;
  }

  .ct-store-badge:hover img { opacity: 0.8; }

  .ct-hero-note {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: #94a3b8;
  }

  .ct-hero-icon-wrap { flex-shrink: 0; }

  .ct-hero-icon {
    width: 180px;
    height: 180px;
    border-radius: 40px;
    box-shadow: 0 20px 60px rgba(6, 182, 212, 0.2), 0 0 80px rgba(6, 182, 212, 0.08);
  }

  @media (min-width: 768px) {
    .ct-hero-icon {
      width: 240px;
      height: 240px;
      border-radius: 54px;
    }
  }

  /* ─── Screenshots Gallery ─── */
  .ct-screenshots {
    background: var(--ct-bg);
    padding: 4rem 0;
    overflow: hidden;
  }

  .ct-screenshots-inner {
    max-width: 72rem;
    margin: 0 auto;
  }

  .ct-screenshots-scroll {
    display: flex;
    gap: 1.25rem;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    padding: 0 1.5rem 1rem;
    scrollbar-width: none;
  }

  .ct-screenshots-scroll::-webkit-scrollbar {
    display: none;
  }

  @media (min-width: 640px) {
    .ct-screenshots-scroll { padding: 0 2rem 1rem; }
  }

  @media (min-width: 1024px) {
    .ct-screenshots-scroll { padding: 0 3rem 1rem; }
  }

  .ct-screenshot {
    flex-shrink: 0;
    width: 220px;
    border-radius: 12px;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
    scroll-snap-align: start;
  }

  @media (min-width: 768px) {
    .ct-screenshot { width: 260px; }
  }

  /* ─── Containers ─── */
  .ct-container {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .ct-container-wide {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  @media (min-width: 640px) {
    .ct-container { padding: 0 2rem; }
    .ct-container-wide { padding: 0 2rem; }
  }

  @media (min-width: 1024px) {
    .ct-container { padding: 0 3rem; }
    .ct-container-wide { padding: 0 3rem; }
  }

  /* ─── Features — 2-col, icon-led ─── */
  .ct-features-section {
    background: var(--ct-surface);
    padding: 5rem 0;
  }

  .ct-features-header {
    margin-bottom: 3.5rem;
    max-width: 520px;
  }

  .ct-label-dark {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6875rem;
    letter-spacing: 0.12em;
    color: var(--ct-accent);
    display: block;
    margin-bottom: 1rem;
  }

  .ct-features-title {
    font-size: clamp(1.75rem, 4vw, 3rem);
    color: var(--ct-text);
    margin-bottom: 1rem;
  }

  .ct-features {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1px;
    background: var(--ct-border);
    border: 1px solid var(--ct-border);
    border-radius: 16px;
    overflow: hidden;
  }

  @media (min-width: 640px) {
    .ct-features { grid-template-columns: 1fr 1fr; }
  }

  .ct-feature {
    padding: 2rem;
    background: var(--ct-surface);
  }

  .ct-feature-icon {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--ct-feature-icon-bg);
    border: 1px solid var(--ct-feature-icon-border);
    border-radius: 10px;
    color: var(--ct-accent-deep);
    margin-bottom: 1.25rem;
  }

  .ct-feature h3 {
    font-family: 'Satoshi', sans-serif;
    font-size: 1.0625rem;
    font-weight: 700;
    color: var(--ct-text);
    margin-bottom: 0.625rem;
    letter-spacing: 0;
    line-height: 1.4;
  }

  .ct-feature p {
    color: var(--ct-text-muted);
    line-height: 1.6;
    font-size: 0.9375rem;
    margin: 0;
  }

  /* ─── Privacy Banner — Full-width dark (stays dark in both modes) ─── */
  .ct-privacy-banner {
    background: #0a0f1a;
    padding: 5rem 0;
    border-top: 1px solid rgba(148, 163, 184, 0.08);
    border-bottom: 1px solid rgba(148, 163, 184, 0.08);
  }

  .ct-privacy-inner {
    text-align: center;
  }

  .ct-privacy-title {
    font-size: clamp(2rem, 5vw, 3rem);
    color: #f1f5f9;
    margin-top: 1rem;
    margin-bottom: 1.25rem;
    letter-spacing: -0.02em;
  }

  .ct-privacy-text {
    font-size: 1.0625rem;
    color: #94a3b8;
    line-height: 1.8;
    margin-bottom: 2rem;
  }

  .ct-privacy-link {
    font-family: 'Satoshi', sans-serif;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--ct-accent);
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .ct-privacy-link:hover {
    opacity: 0.7;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  /* ─── CTA ─── */
  .ct-cta {
    padding: 6rem 0;
    background: var(--ct-accent-bg);
    position: relative;
    overflow: hidden;
  }

  .ct-cta-inner {
    text-align: center;
    max-width: 480px;
    margin: 0 auto;
    position: relative;
  }

  .ct-cta-title {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--ct-text);
    margin-bottom: 1rem;
  }

  .ct-cta-text {
    font-size: 1.125rem;
    color: var(--ct-text-muted);
    margin-bottom: 2rem;
  }

  /* ─── Footer ─── */
  .ct-footer {
    background: var(--ct-bg);
    border-top: 1px solid var(--ct-border);
    padding: 2rem 0;
  }

  .ct-footer-inner {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  @media (min-width: 640px) {
    .ct-footer-inner {
      flex-direction: row;
      justify-content: space-between;
      padding: 0 2rem;
    }
  }

  @media (min-width: 1024px) {
    .ct-footer-inner { padding: 0 3rem; }
  }

  .ct-footer-links {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.875rem;
  }

  .ct-footer-links a {
    color: var(--ct-text-muted);
    text-decoration: none;
    transition: color 0.2s;
  }

  .ct-footer-links a:hover {
    color: var(--ct-accent);
  }

  .ct-footer-dot {
    color: var(--ct-text-muted);
    opacity: 0.4;
  }

  .ct-footer-brand {
    font-size: 0.8125rem;
    color: var(--ct-text-muted);
    opacity: 0.6;
  }

  .ct-footer-brand a {
    color: var(--ct-text-muted);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color 0.2s;
  }

  .ct-footer-brand a:hover {
    color: var(--ct-accent);
  }
</style>
