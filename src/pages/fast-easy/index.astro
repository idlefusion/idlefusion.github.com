---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Fast Easy — Fasting Timer for iOS"
  description="A simple, beautiful fasting timer. No accounts, no tracking, no subscriptions. Just start your fast."
  ogImage="/img/fast-easy/icon.png"
  ogUrl="https://idlefusion.com/fast-easy"
>

  <!-- App Nav -->
  <nav class="fe-nav">
    <div class="fe-nav-inner">
      <a href="/fast-easy" class="fe-logo">Fast Easy</a>
      <a href="https://apps.apple.com/app/fast-easy-fasting-timer/id6745400613" class="fe-nav-cta" target="_blank" rel="noopener noreferrer">
        <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="40" />
      </a>
    </div>
  </nav>

  <main>
    <!-- Hero — Minimal, centered, tons of whitespace -->
    <section class="fe-hero">
      <div class="fe-hero-inner">
        <img src="/img/fast-easy/icon.png" alt="Fast Easy app icon" class="fe-hero-icon" width="240" height="240" />
        <h1 class="fe-hero-title">
          Fasting,<br/>made <em>easy.</em>
        </h1>
        <p class="fe-hero-subtitle">
          A simple, beautiful fasting timer for iOS.<br/>
          No accounts. No tracking. No subscriptions.
        </p>
        <div class="fe-hero-actions">
          <a href="https://apps.apple.com/app/fast-easy-fasting-timer/id6745400613" class="fe-store-badge" target="_blank" rel="noopener noreferrer">
            <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
          </a>
          <span class="fe-hero-note">Free &middot; No account required</span>
        </div>
      </div>
    </section>

    <!-- Features — Vertical editorial list, not a grid -->
    <section class="fe-section">
      <div class="fe-container">
        <div class="fe-section-header">
          <span class="fe-label">FEATURES</span>
          <h2 class="fe-section-title">Simple by design.</h2>
        </div>

        <div class="fe-feature-list">
          <div class="fe-feature-item">
            <div class="fe-feature-num">01</div>
            <div class="fe-feature-body">
              <h3>Fasting Presets</h3>
              <p>12:12, 16:8, 18:6, 20:4, OMAD, and 36-hour fasts. Pick one and go — or count up from zero.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">02</div>
            <div class="fe-feature-body">
              <h3>10 Timer Skins</h3>
              <p>Swipe through visual styles — minimal arcs, gradient rings, gauges, and more. Pick the one that fits your vibe.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">03</div>
            <div class="fe-feature-body">
              <h3>Live Activities</h3>
              <p>Track your fast from the lock screen and Dynamic Island without opening the app.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">04</div>
            <div class="fe-feature-body">
              <h3>Statistics & Streaks</h3>
              <p>Weekly charts, completion rates, total fasting hours, and current streak — all computed locally.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">05</div>
            <div class="fe-feature-body">
              <h3>History</h3>
              <p>Every completed fast is saved. Review your progress, see your longest fasts, and stay motivated.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">06</div>
            <div class="fe-feature-body">
              <h3>Completely Offline</h3>
              <p>No servers, no cloud sync, no network requests. Your data stays on your device. Period.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Privacy — Full-width dark callout -->
    <section class="fe-privacy-banner">
      <div class="fe-container">
        <div class="fe-privacy-inner">
          <h2 class="fe-privacy-title">Zero data collection.</h2>
          <p class="fe-privacy-text">
            No analytics. No ads. No tracking pixels. No telemetry.<br/>
            Fast Easy doesn't even make network requests.
          </p>
          <a href="/fast-easy/privacy" class="fe-privacy-link">Read the privacy policy &rarr;</a>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="fe-cta">
      <div class="fe-container">
        <div class="fe-cta-inner">
          <h2 class="fe-cta-title">Start fasting today.</h2>
          <p class="fe-cta-text">Free on the App Store. No account required.</p>
          <a href="https://apps.apple.com/app/fast-easy-fasting-timer/id6745400613" class="fe-store-badge" target="_blank" rel="noopener noreferrer">
            <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
          </a>
        </div>
      </div>
    </section>
  </main>

  <!-- App Footer -->
  <footer class="fe-footer">
    <div class="fe-footer-inner">
      <div class="fe-footer-links">
        <a href="/fast-easy/support">Support</a>
        <span class="fe-footer-dot">&middot;</span>
        <a href="/fast-easy/privacy">Privacy Policy</a>
      </div>
      <div class="fe-footer-brand">
        An <a href="/">Idle Fusion</a> app
      </div>
    </div>
  </footer>

</BaseLayout>

<style>
  /* ═══════════════════════════════════════════════════════
     FAST EASY — Warm Amber / Gold Theme
     Zen, editorial, single-column, lots of whitespace
     ═══════════════════════════════════════════════════════ */

  :root {
    --fe-accent: #f59e0b;
    --fe-accent-soft: #fbbf24;
    --fe-accent-deep: #d97706;
    --fe-accent-bg: #fffbeb;
    --fe-dark-bg: #1c1308;
    --fe-dark-text: #fef3c7;
    --fe-dark-muted: #a3894d;
  }

  .dark {
    --fe-accent-bg: #451a03;
  }

  /* ─── Nav ─── */
  .fe-nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: var(--bg-primary);
    border-bottom: 1px solid var(--border-color);
    backdrop-filter: blur(12px);
  }

  .fe-nav-inner {
    max-width: 40rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 3.5rem;
  }

  @media (min-width: 640px) {
    .fe-nav-inner { padding: 0 2rem; }
  }

  .fe-logo {
    font-family: 'Satoshi', sans-serif;
    font-size: 0.9375rem;
    font-weight: 700;
    color: var(--fe-accent-deep);
    text-decoration: none;
    letter-spacing: -0.01em;
  }

  .dark .fe-logo {
    color: var(--fe-accent);
  }

  .fe-nav-cta img {
    height: 32px;
    transition: opacity 0.2s;
  }

  .fe-nav-cta:hover img {
    opacity: 0.7;
  }

  /* ─── Hero — Centered, meditative, generous spacing ─── */
  .fe-hero {
    padding: 6rem 0 7rem;
    text-align: center;
    background: var(--bg-primary);
  }

  .fe-hero-inner {
    max-width: 540px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .fe-hero-icon {
    width: 120px;
    height: 120px;
    border-radius: 28px;
    box-shadow: 0 20px 60px rgba(245, 158, 11, 0.15), 0 8px 20px rgba(245, 158, 11, 0.1);
    margin-bottom: 3rem;
  }

  @media (min-width: 768px) {
    .fe-hero-icon {
      width: 148px;
      height: 148px;
      border-radius: 34px;
    }
  }

  .fe-hero-title {
    font-size: clamp(2.75rem, 7vw, 4.5rem);
    color: var(--text-primary);
    margin-bottom: 1.75rem;
    line-height: 1.0;
    letter-spacing: -0.03em;
  }

  .fe-hero-title em {
    font-style: italic;
    color: var(--fe-accent-deep);
  }

  .dark .fe-hero-title em {
    color: var(--fe-accent);
  }

  .fe-hero-subtitle {
    font-size: 1.0625rem;
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: 3rem;
  }

  .fe-hero-actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }

  .fe-store-badge img {
    height: 54px;
    transition: opacity 0.2s;
  }

  .fe-store-badge:hover img { opacity: 0.8; }

  .fe-hero-note {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  /* ─── Sections ─── */
  .fe-section {
    padding: 5rem 0;
  }

  .fe-container {
    max-width: 40rem;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  @media (min-width: 640px) {
    .fe-container { padding: 0 2rem; }
  }

  .fe-section-header {
    margin-bottom: 3rem;
  }

  .fe-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6875rem;
    letter-spacing: 0.12em;
    color: var(--fe-accent-deep);
    display: block;
    margin-bottom: 1rem;
  }

  .dark .fe-label {
    color: var(--fe-accent);
  }

  .fe-section-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    color: var(--text-primary);
    letter-spacing: -0.02em;
  }

  /* ─── Feature List — Vertical, numbered, editorial ─── */
  .fe-feature-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .fe-feature-item {
    display: flex;
    align-items: baseline;
    gap: 1.5rem;
    padding: 1.75rem 0;
    border-bottom: 1px solid var(--border-color);
  }

  .fe-feature-item:first-child {
    border-top: 1px solid var(--border-color);
  }

  .fe-feature-num {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--fe-accent-deep);
    flex-shrink: 0;
    width: 2rem;
    letter-spacing: 0.05em;
  }

  .dark .fe-feature-num {
    color: var(--fe-accent);
  }

  .fe-feature-body h3 {
    font-family: 'Satoshi', sans-serif;
    font-size: 1.0625rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
    letter-spacing: 0;
    line-height: 1.4;
  }

  .fe-feature-body p {
    color: var(--text-secondary);
    line-height: 1.7;
    font-size: 0.9375rem;
    margin: 0;
  }

  /* ─── Privacy Banner — Full-width dark callout ─── */
  .fe-privacy-banner {
    background: var(--fe-dark-bg);
    padding: 5rem 0;
  }

  .dark .fe-privacy-banner {
    background: #1a1301;
    border-top: 1px solid rgba(245, 158, 11, 0.08);
    border-bottom: 1px solid rgba(245, 158, 11, 0.08);
  }

  .fe-privacy-inner {
    text-align: center;
  }

  .fe-privacy-title {
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--fe-dark-text);
    margin-bottom: 1.25rem;
    letter-spacing: -0.02em;
  }

  .fe-privacy-text {
    font-size: 1.0625rem;
    color: var(--fe-dark-muted);
    line-height: 1.8;
    margin-bottom: 2rem;
  }

  .fe-privacy-link {
    font-family: 'Satoshi', sans-serif;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--fe-accent);
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .fe-privacy-link:hover {
    opacity: 0.7;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  /* ─── CTA ─── */
  .fe-cta {
    padding: 6rem 0;
    background: var(--bg-primary);
  }

  .fe-cta-inner {
    text-align: center;
  }

  .fe-cta-title {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--text-primary);
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }

  .fe-cta-text {
    font-size: 1.125rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  /* ─── Footer ─── */
  .fe-footer {
    border-top: 1px solid var(--border-color);
    padding: 2rem 0;
  }

  .fe-footer-inner {
    max-width: 40rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  @media (min-width: 640px) {
    .fe-footer-inner {
      flex-direction: row;
      justify-content: space-between;
      padding: 0 2rem;
    }
  }

  .fe-footer-links {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.875rem;
  }

  .fe-footer-links a {
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.2s;
  }

  .fe-footer-links a:hover {
    color: var(--fe-accent);
  }

  .fe-footer-dot {
    color: var(--text-muted);
    opacity: 0.4;
  }

  .fe-footer-brand {
    font-size: 0.8125rem;
    color: var(--text-muted);
    opacity: 0.6;
  }

  .fe-footer-brand a {
    color: var(--text-muted);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color 0.2s;
  }

  .fe-footer-brand a:hover {
    color: var(--fe-accent);
  }
</style>
