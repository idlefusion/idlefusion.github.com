---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
---

<BaseLayout
  title="Fast Easy — Fasting Timer for iOS"
  description="A simple, beautiful fasting timer. No accounts, no tracking, no subscriptions. Just start your fast."
  ogImage="/img/fast-easy/icon.png"
  ogUrl="https://idlefusion.com/fast-easy"
>

  <!-- App Nav -->
  <nav class="fe-nav">
    <div class="fe-nav-inner">
      <a href="/fast-easy" class="fe-logo">Fast Easy</a>
      <div class="fe-nav-actions">
        <ThemeToggle />
        <a href="https://apps.apple.com/app/fast-easy-fasting-timer/id6745400613" class="fe-nav-cta" target="_blank" rel="noopener noreferrer">
          <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="40" />
        </a>
      </div>
    </div>
  </nav>

  <main>
    <!-- Hero — Minimal, centered, tons of whitespace -->
    <section class="fe-hero">
      <div class="fe-hero-inner">
        <img src="/img/fast-easy/icon.png" alt="Fast Easy app icon" class="fe-hero-icon" width="240" height="240" />
        <h1 class="fe-hero-title">
          Fasting,<br/>made <em>easy.</em>
        </h1>
        <p class="fe-hero-subtitle">
          A simple, beautiful fasting timer for iOS.<br/>
          No accounts. No tracking. No subscriptions.
        </p>
        <div class="fe-hero-actions">
          <a href="https://apps.apple.com/app/fast-easy-fasting-timer/id6745400613" class="fe-store-badge" target="_blank" rel="noopener noreferrer">
            <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
          </a>
          <span class="fe-hero-note">Free &middot; No account required</span>
        </div>
      </div>
    </section>

    <!-- Features — Vertical editorial list, not a grid -->
    <section class="fe-section">
      <div class="fe-container">
        <div class="fe-section-header">
          <span class="fe-label">FEATURES</span>
          <h2 class="fe-section-title">Simple by design.</h2>
        </div>

        <div class="fe-feature-list">
          <div class="fe-feature-item">
            <div class="fe-feature-num">01</div>
            <div class="fe-feature-body">
              <h3>Fasting Presets</h3>
              <p>12:12, 16:8, 18:6, 20:4, OMAD, and 36-hour fasts. Pick one and go — or count up from zero.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">02</div>
            <div class="fe-feature-body">
              <h3>10 Timer Skins</h3>
              <p>Swipe through visual styles — minimal arcs, gradient rings, gauges, and more. Pick the one that fits your vibe.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">03</div>
            <div class="fe-feature-body">
              <h3>Live Activities</h3>
              <p>Track your fast from the lock screen and Dynamic Island without opening the app.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">04</div>
            <div class="fe-feature-body">
              <h3>Statistics & Streaks</h3>
              <p>Weekly charts, completion rates, total fasting hours, and current streak — all computed locally.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">05</div>
            <div class="fe-feature-body">
              <h3>History</h3>
              <p>Every completed fast is saved. Review your progress, see your longest fasts, and stay motivated.</p>
            </div>
          </div>

          <div class="fe-feature-item">
            <div class="fe-feature-num">06</div>
            <div class="fe-feature-body">
              <h3>Completely Offline</h3>
              <p>No servers, no cloud sync, no network requests. Your data stays on your device. Period.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Privacy — Full-width dark callout -->
    <section class="fe-privacy-banner">
      <div class="fe-container">
        <div class="fe-privacy-inner">
          <h2 class="fe-privacy-title">Zero data collection.</h2>
          <p class="fe-privacy-text">
            No analytics. No ads. No tracking pixels. No telemetry.<br/>
            Fast Easy doesn't even make network requests.
          </p>
          <a href="/fast-easy/privacy" class="fe-privacy-link">Read the privacy policy &rarr;</a>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="fe-cta">
      <div class="fe-container">
        <div class="fe-cta-inner">
          <h2 class="fe-cta-title">Start fasting today.</h2>
          <p class="fe-cta-text">Free on the App Store. No account required.</p>
          <a href="https://apps.apple.com/app/fast-easy-fasting-timer/id6745400613" class="fe-store-badge" target="_blank" rel="noopener noreferrer">
            <img src="https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg" alt="Download on the App Store" height="54" />
          </a>
        </div>
      </div>
    </section>
  </main>

  <!-- App Footer -->
  <footer class="fe-footer">
    <div class="fe-footer-inner">
      <div class="fe-footer-links">
        <a href="/fast-easy/support">Support</a>
        <span class="fe-footer-dot">&middot;</span>
        <a href="/fast-easy/privacy">Privacy Policy</a>
      </div>
      <div class="fe-footer-brand">
        An <a href="/">Idle Fusion</a> app
      </div>
    </div>
  </footer>

</BaseLayout>

<style>
  /* ═══════════════════════════════════════════════════════
     FAST EASY — Teal Theme
     Self-contained color system, zen editorial layout
     ═══════════════════════════════════════════════════════ */

  :global(:root) {
    --fe-accent: #14b8a6;
    --fe-accent-deep: #0d9488;
    --fe-bg: #fafaf9;
    --fe-bg-alt: #f0fdfa;
    --fe-bg-card: #ffffff;
    --fe-text: #0a0a0a;
    --fe-text-secondary: #44403c;
    --fe-text-muted: #78716c;
    --fe-border: #e7e5e4;
    --fe-privacy-bg: #0a1a19;
    --fe-privacy-text: #ccfbf1;
    --fe-privacy-muted: #5b8a84;
  }

  :global(.dark) {
    --fe-accent: #2dd4bf;
    --fe-accent-deep: #14b8a6;
    --fe-bg: #0a1615;
    --fe-bg-alt: #0f201e;
    --fe-bg-card: #132c29;
    --fe-text: #e2e8f0;
    --fe-text-secondary: #94a3b8;
    --fe-text-muted: #64748b;
    --fe-border: rgba(20, 184, 166, 0.12);
    --fe-privacy-bg: #071210;
    --fe-privacy-text: #ccfbf1;
    --fe-privacy-muted: #5b8a84;
  }

  /* ─── Nav ─── */
  .fe-nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: var(--fe-bg);
    border-bottom: 1px solid var(--fe-border);
    backdrop-filter: blur(12px);
  }

  .fe-nav-inner {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 3.5rem;
  }

  @media (min-width: 640px) {
    .fe-nav-inner { padding: 0 2rem; }
  }

  @media (min-width: 1024px) {
    .fe-nav-inner { padding: 0 3rem; }
  }

  .fe-logo {
    font-family: 'Satoshi', sans-serif;
    font-size: 0.9375rem;
    font-weight: 700;
    color: var(--fe-accent-deep);
    text-decoration: none;
    letter-spacing: -0.01em;
  }

  .fe-nav-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .fe-nav-cta img {
    height: 32px;
    transition: opacity 0.2s;
  }

  .fe-nav-cta:hover img {
    opacity: 0.7;
  }

  /* ─── Hero — Centered, meditative, generous spacing ─── */
  .fe-hero {
    padding: 6rem 0 7rem;
    text-align: center;
    background: var(--fe-bg-alt);
  }

  .fe-hero-inner {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  @media (min-width: 640px) {
    .fe-hero-inner { padding: 0 2rem; }
  }

  @media (min-width: 1024px) {
    .fe-hero-inner { padding: 0 3rem; }
  }

  .fe-hero-icon {
    width: 120px;
    height: 120px;
    border-radius: 28px;
    box-shadow: 0 20px 60px rgba(20, 184, 166, 0.15), 0 8px 20px rgba(20, 184, 166, 0.1);
    margin-bottom: 3rem;
  }

  @media (min-width: 768px) {
    .fe-hero-icon {
      width: 148px;
      height: 148px;
      border-radius: 34px;
    }
  }

  .fe-hero-title {
    font-size: clamp(2.75rem, 7vw, 4.5rem);
    color: var(--fe-text);
    margin-bottom: 1.75rem;
    line-height: 1.0;
    letter-spacing: -0.03em;
  }

  .fe-hero-title em {
    font-style: italic;
    color: var(--fe-accent-deep);
  }

  .fe-hero-subtitle {
    font-size: 1.0625rem;
    color: var(--fe-text-secondary);
    line-height: 1.8;
    margin-bottom: 3rem;
  }

  .fe-hero-actions {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }

  .fe-store-badge img {
    height: 54px;
    transition: opacity 0.2s;
  }

  .fe-store-badge:hover img { opacity: 0.8; }

  .fe-hero-note {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.75rem;
    color: var(--fe-text-muted);
  }

  /* ─── Sections ─── */
  .fe-section {
    padding: 5rem 0;
    background: var(--fe-bg);
  }

  .fe-container {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  @media (min-width: 640px) {
    .fe-container { padding: 0 2rem; }
  }

  @media (min-width: 1024px) {
    .fe-container { padding: 0 3rem; }
  }

  .fe-section-header {
    margin-bottom: 3rem;
  }

  .fe-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.6875rem;
    letter-spacing: 0.12em;
    color: var(--fe-accent-deep);
    display: block;
    margin-bottom: 1rem;
  }

  .fe-section-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    color: var(--fe-text);
    letter-spacing: -0.02em;
  }

  /* ─── Feature List — Vertical, numbered, editorial ─── */
  .fe-feature-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .fe-feature-item {
    display: flex;
    align-items: baseline;
    gap: 1.5rem;
    padding: 1.75rem 0;
    border-bottom: 1px solid var(--fe-border);
  }

  .fe-feature-item:first-child {
    border-top: 1px solid var(--fe-border);
  }

  .fe-feature-num {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--fe-accent);
    flex-shrink: 0;
    width: 2rem;
    letter-spacing: 0.05em;
  }

  .fe-feature-body h3 {
    font-family: 'Satoshi', sans-serif;
    font-size: 1.0625rem;
    font-weight: 700;
    color: var(--fe-text);
    margin-bottom: 0.5rem;
    letter-spacing: 0;
    line-height: 1.4;
  }

  .fe-feature-body p {
    color: var(--fe-text-secondary);
    line-height: 1.7;
    font-size: 0.9375rem;
    margin: 0;
  }

  /* ─── Privacy Banner — Always dark ─── */
  .fe-privacy-banner {
    background: var(--fe-privacy-bg);
    padding: 5rem 0;
  }

  .fe-privacy-inner {
    text-align: center;
  }

  .fe-privacy-title {
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--fe-privacy-text);
    margin-bottom: 1.25rem;
    letter-spacing: -0.02em;
  }

  .fe-privacy-text {
    font-size: 1.0625rem;
    color: var(--fe-privacy-muted);
    line-height: 1.8;
    margin-bottom: 2rem;
  }

  .fe-privacy-link {
    font-family: 'Satoshi', sans-serif;
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--fe-accent);
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .fe-privacy-link:hover {
    opacity: 0.7;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  /* ─── CTA ─── */
  .fe-cta {
    padding: 6rem 0;
    background: var(--fe-bg-alt);
  }

  .fe-cta-inner {
    text-align: center;
  }

  .fe-cta-title {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--fe-text);
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }

  .fe-cta-text {
    font-size: 1.125rem;
    color: var(--fe-text-secondary);
    margin-bottom: 2rem;
  }

  /* ─── Footer ─── */
  .fe-footer {
    background: var(--fe-bg);
    border-top: 1px solid var(--fe-border);
    padding: 2rem 0;
  }

  .fe-footer-inner {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  @media (min-width: 640px) {
    .fe-footer-inner {
      flex-direction: row;
      justify-content: space-between;
      padding: 0 2rem;
    }
  }

  @media (min-width: 1024px) {
    .fe-footer-inner { padding: 0 3rem; }
  }

  .fe-footer-links {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 0.875rem;
  }

  .fe-footer-links a {
    color: var(--fe-text-muted);
    text-decoration: none;
    transition: color 0.2s;
  }

  .fe-footer-links a:hover {
    color: var(--fe-accent);
  }

  .fe-footer-dot {
    color: var(--fe-text-muted);
    opacity: 0.4;
  }

  .fe-footer-brand {
    font-size: 0.8125rem;
    color: var(--fe-text-muted);
    opacity: 0.6;
  }

  .fe-footer-brand a {
    color: var(--fe-text-muted);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color 0.2s;
  }

  .fe-footer-brand a:hover {
    color: var(--fe-accent);
  }
</style>
